---
// src/layouts/Layout.astro
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="zh-cn">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<title>{title} - 我的博客</title>
        <style is:global>
            /* 简单的全局样式 */
            body { font-family: system-ui, sans-serif; max-width: 800px; margin: 0 auto; padding: 20px; line-height: 1.6; color: #333; }
            a { color: #2563eb; text-decoration: none; }
            a:hover { text-decoration: underline; }
            pre { background: #f4f4f5; padding: 15px; border-radius: 8px; overflow-x: auto; }
            img { max-width: 100%; height: auto; border-radius: 8px; }
            nav { margin-bottom: 40px; border-bottom: 1px solid #eee; padding-bottom: 10px; }
        </style>
	</head>
	<body>
        <nav>
            <a href="/" style="font-weight: bold; font-size: 1.2rem;">🏠 My Blog</a>
        </nav>
		<slot />
	</body>
</html>