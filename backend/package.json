{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "target": "bun",
  "scripts": {
    "dev": "bun run index.ts --watch",
    "build": "bun build ./index --compile --outfile backend",
    "start": "bun run backend"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}